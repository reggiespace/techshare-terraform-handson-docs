// will it work? lets give a try

// first, lets run terraform init to install the providers
terraform init

//show the .terraform folder


// and then, lets plan the infrastructure
terraform plan

// nothing has been created so far, but since the plan looks good, it is a good time to commit to git.
// I am not doing that to save time, but I strong recommend you to do at this step